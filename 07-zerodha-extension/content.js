// Wait for the DOM to load
document.addEventListener("DOMContentLoaded", () => {
    // Select all `<div>` elements within #open-position-report
   console.log('some success with content script yay!!!')
   
    // const container = document.querySelector("#open-position-report");
  
    // if (container) {
    //   // Loop through each inner div and add a button
    //   container.querySelectorAll("div").forEach((div) => {
    //     // Check if a button already exists
    //     if (!div.querySelector(".refresh-button")) {
    //       // Create a new button
    //       const button = document.createElement("button");
    //       button.className = "refresh-button";
    //       button.textContent = "🔄";
  
    //       // Add click event to the button
    //       button.addEventListener("click", () => {
    //         const span = div.querySelector("span:nth-of-type(2)");
    //         if (span) {
    //           // Update span with random value (example logic)
    //           span.textContent = Math.floor(Math.random() * 100000);
    //           console.log(`Refreshed value for: ${span.id}`);
    //         }
    //       });
  
    //       // Append the button to the div
    //       div.appendChild(button);
    //     }
    //   });
    // }
  });
  